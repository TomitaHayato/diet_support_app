import { describe, test } from "vitest";

describe('コンポーネント： LogoutForm.jsxのテスト', () => {
  test('要素が正常に表示される', () => {});

  test('ログアウトボタンを押下 => logout() + AuthContextのsetCurrentUser(false)が実行される', () => {});
});
