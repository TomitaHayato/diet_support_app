import { describe, test } from "vitest";

describe('コンポーネント： LoginForm.jsxのテスト', () => {
  test('ログインフォームが表示される', () => {});

  test('バリデーションエラーがerrorに格納される', () => {});

  test('正常に入力してログインボタンを押下 => hundleSubmitのコールバックが発火', () => {});
});
