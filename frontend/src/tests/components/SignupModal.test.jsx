import { describe, test } from "vitest";

describe('コンポーネント： SignupModal.jsxのテスト', () => {
  test('初期表示', () => {});

  test('新規登録ボタンを押下 => フォームモーダルが表示される', () => {});

  test('不正な入力値を入力した場合、errorにエラーメッセージが格納', () => {});

  test('正常値が入力された場合、signup()が呼び出される => setCurrentUser(), settingAuthTokenToCookie()が実行される', () => {});
});
