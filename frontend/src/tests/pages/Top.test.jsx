import { cleanup } from "@testing-library/react";
import { afterEach, beforeEach, describe, test } from "vitest";

// 子コンポーネントも合わせてテストする（コンポーネント間の機能が繋がっているから）
describe('コンポーネント： Top.jsxのテスト', () => {
  beforeEach(() => {});

  afterEach(() => cleanup());

  test('初期表示', async() => {});
  test('', async() => {});
  test('', async() => {});
  test('', async() => {});
  test('', async() => {});
  test('', async() => {});
});